source "https://rubygems.org"

# For local development. GitHub Pages will build with its own environment.
gem "github-pages", group: :jekyll_plugins

# Required for Ruby >= 3 when serving locally
gem "webrick", "~> 1.8"

# Needed for Minimal Mistakes caching includes locally
gem "jekyll-include-cache"
